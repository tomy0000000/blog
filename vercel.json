{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "bash -c 'git diff --quiet HEAD^ HEAD -- api/ || exit 1'",
  "functions": {
    "api/newsletter-signup.ts": {
      "maxDuration": 30
    },
    "api/health.ts": {
      "maxDuration": 30
    }
  },
  "redirects": [
    { "source": "/", "destination": "https://blog.tomy.me", "permanent": true }
  ]
}
